allprojects{
  apply plugin: "idea"
}

subprojects {
  repositories {
    mavenCentral()
  }
}

task printChildProjects<<{
  childProjects.each{
    println it.value.class.name
  }
}

task printSubProjects<<{
  subprojects.each{
    println it.class.name
  }
}


